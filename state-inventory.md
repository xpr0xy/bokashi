# State inventory

- app: booted, restored, invalid-shared-state, viewport-bound desktop, document-flow tablet/mobile
- mode: make, traditional, mono, air, dither, cube, image, plane, catalogue, harmony, bands, contrast
- recipe browse: selected, family filtered, type filtered, no matches
- stops: 2–5, active stop, reversed, distributed, position validation
- mono: base, shadow endpoint, paper endpoint
- air: diffusion, spread, strength, origin
- dither: pattern, cell scale, threshold bias, angle
- cube: speed, depth count, direction, reduced-motion still frame
- image: idle, decoding, sampled, invalid type, unreadable, palette count, luminance ordering
- audio: idle, decoding, analysed, unsupported
- colour plane: HSL projection, RGB projection, dragging, selected point, keyboard traversal
- catalogue browse: query, favourites only, sort order, no matches, copied hex
- harmony: base colour, scheme, hue spread, catalogue-quantised palette
- bands: palette, angle, scale, gap, offset
- contrast: foreground, background, ratio, AA/AAA normal and large verdicts
- export: closed, ready, rendering, downloaded, failed, format unsupported
- clipboard: idle, copied, denied fallback
- persistence: available, unavailable/private mode
- reduced motion: normal, reduced

No state may blank the current artwork. Errors appear beside the initiating control and retain the previous valid output. Every visible reproducible renderer parameter must round-trip through the shared state token. Browse-only search text may remain ephemeral.
