{
  "name": "bokashi",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
